package com.ubiquibit.buoy.jobs

object ResetBuoyFailures {

  // if a buoy is marked "failed" (perhaps b/c of a network interruption to this service),
  // then this script resets the clock for it and all other buoy stations

}
